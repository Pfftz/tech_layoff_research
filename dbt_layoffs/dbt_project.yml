name: 'dbt_layoffs'
version: '1.0.0'
config-version: 2

profile: 'tech_layoffs_project'

model-paths: ["models"]

models:
  dbt_layoffs:
    staging:
      +materialized: view
      +schema: staging
    silver:
      +materialized: table
      +schema: silver
    gold:
      +materialized: table
      +schema: gold

